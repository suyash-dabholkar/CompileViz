"""
FastAPI routers.

Each milestone that exposes new functionality to the frontend gets its
own router module here (e.g. regex_routes.py, grammar_routes.py,
compiler_routes.py), included from app.main in that milestone's PR.
"""
